{
  "name": "jate",
  "version": "1.0.0",
  "description": "## 19 Progressive Web Applications (PWA): Text Editor",
  "main": "server.js",
  "scripts": {
    "start:dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run build && npm run server",
    "server": "node server/server.js",
    "build": "cd client && npm run build",
    "install": "cd client && npm install",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "css-loader": "^6.8.1",
    "nodemon": "^2.0.4",
    "style-loader": "^3.3.3"
  }
}
